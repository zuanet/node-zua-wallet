{
  "name": "@zua/grpc-node",
  "version": "1.0.8",
  "description": "Node gRPC for zua-wallet",
  "main": "dist/index.js",
  "author": "ASPECTRON Inc.",
  "license": "ISC",
  "scripts": {
    "watch": "tsc --watch",
    "prepublishOnly": "rm -rf ./dist && rm -f package-lock.json && npm install --also=dev && tsc"
  },
  "dependencies": {
    "@zua/grpc": ">=1.4.0"
  },
  "devDependencies": {
    "@types/node": "^14.14.9",
    "typescript": "^4.0.5"
  },
  "files": [
    "dist",
    "lib",
    "types",
    "index.ts",
    "tsconfig.json"
  ]
}
